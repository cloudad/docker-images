---
- hosts: all
  gather_facts: no
  connection: local

  tags:
    - bootstrap

  pre_tasks:
    - name: begin bootstrap
      debug:
        msg: Bootstraping directory '{{ target_path|path_abspath }}'

  roles:
    - role: local_roles/bootstrap/
